# Générer la documentation
dbt docs generate

# Lancer le serveur d'UI (accessible à http://localhost:8080)
dbt docs serve

dbt run          # Exécuter les modèles
dbt run --select # Exécuter une table précise ou un modèle
dbt test         # Lancer les tests
dbt build        # Run + test

dbt debug        # vérification 

#etape 1
PostgreSQL en Docker (port 5433)
dbt Core installé localement
.dbt/profiles.yml configuré
p8/dbt_project.yml créé
Structure des dossiers prête

#etape 2
creation model stagging
standardisation champs et formats

#etape 3
creation model intermediaire
création nouvelle mesure
merge des données

#etape 4
creation model marts
table finale avec id